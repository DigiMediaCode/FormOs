import "server-only";

export function encryptIntegrationToken(token: string) {
  // Central placeholder for future encryption-at-rest.
  return token;
}

export function decryptIntegrationToken(token: string) {
  // Central placeholder for future encryption-at-rest.
  return token;
}
